#!/usr/bin/env bash
# Run every method (4 reasoning + traditional baseline) on a dataset,
# sequentially, with a total-time banner. Wraps run_{pi,rrf,gptree,rrm}.py
# and run_traditional.py. Expect ~35 h (Movie) / ~70 h (VCBench) on an
# M2 Mac with local qwen2.5-coder:14b.
#
# Implementation lands in Stage 6.
echo "run_all.sh: implementation lands in Stage 6" >&2
exit 1
